# Models live outside the repo -- never commit multi-GB GGUFs
*.gguf

# Scratch
*.tmp
*.swp

# Generated sites: keep the source (it's quality evidence), never the deps
sites/*/*/node_modules/
sites/*/.pi-agent/sessions/
